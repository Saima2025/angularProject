import { Routes } from '@angular/router';

export const routes: Routes = [
    // {
    //     path: 'requests',
    //     loadChildren: () => import('./trip_request_list/tripList.module').then((m) => m.TripListModule),
    //     canActivate: [AuthGuard]
    // },
];
